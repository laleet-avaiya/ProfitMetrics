rules_version = '2';

// TEMPORARY — permissive rules for development / unblock.
// Any signed-in user can read and write storage files.

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
